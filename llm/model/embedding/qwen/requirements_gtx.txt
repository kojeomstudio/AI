# GTX GPU 최적화를 위한 의존성 패키지
# PyTorch with CUDA support (GTX 시리즈 호환)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers and related
transformers>=4.30.0
accelerate>=0.20.0
peft>=0.4.0

# Data processing
datasets>=2.12.0
tokenizers>=0.13.0

# Training utilities
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
pandas>=2.0.0

# Memory and performance
psutil>=5.9.0
tqdm>=4.65.0

# HuggingFace Hub
huggingface-hub>=0.15.0

# Optional: For advanced optimizations
# bitsandbytes>=0.39.0  # 8-bit training (GTX 시리즈에서는 제한적)
# deepspeed>=0.9.0      # 분산 훈련 (선택사항)

# Evaluation and metrics
evaluate>=0.4.0
sentence-transformers>=2.2.0

# File handling
PyYAML>=6.0
safetensors>=0.3.0

# Monitoring (optional)
# wandb>=0.15.0         # 실험 추적
# tensorboard>=2.13.0   # 텐서보드 로깅